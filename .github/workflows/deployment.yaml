name: Deployment workflow
on: push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Run lint
        run: npm run lint
      - name: Run tests
        run: npm run test
      - name: Build app
        run: npm run build

